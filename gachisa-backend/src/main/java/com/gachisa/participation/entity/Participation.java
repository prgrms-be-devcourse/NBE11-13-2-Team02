package com.gachisa.participation.entity;

// TODO: @Entity
// id, groupBuyId(FK), userId(FK), quantity,
// status("참여중"/"확정"/"환불됨"/"취소됨"), participatedAt
// 확정 이후 상태는 취소됨으로 직접 전이 불가 -> 반드시 환불됨을 거침
public class Participation {
}
